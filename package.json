{
  "name": "async-until",
  "version": "4.0.1",
  "description": "Wait until a given callback returns true",
  "repository": "https://github.com/skidding/async-until.git",
  "author": "Ovidiu Cherecheș <hello@ovidiu.ch>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prettify": "prettier --write \"src/**/*.{js,jsx,ts,tsx,json,css,md}\"",
    "check-types": "tsc --noEmit",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "rm -rf dist && yarn tsc -b tsconfig.build.json"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {}
}
